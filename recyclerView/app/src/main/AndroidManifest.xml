<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="recyclerview.materialdesign.interra.com.recyclerview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/star_wars"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LinearLayout">

        </activity>
        <activity android:name=".GridLayout"
            android:label="Movies">

        </activity>
        <activity android:name=".StaggeredLayout"
            android:label="Pixar Characters">

        </activity>
        <activity android:name=".ListView"
            android:label="Countries">

        </activity>
        <activity android:name=".ListViewSecond"
            android:label="@string/countries_second" />
    </application>

</manifest>